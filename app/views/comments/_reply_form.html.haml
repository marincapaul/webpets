.ml-5.text-lg
  = form_with(model: [@post, @comment], data: {remote: true} ) do |f|
    = f.hidden_field :parent_id, value: params[:parent_id]
    .form-group
      = f.text_area :body,placeholder: "Write a reply!", class: 'border sm:text-sm rounded-lg block w-full p-2.5 bg-gray-700 border-gray-600 placeholder-gray-400 text-white focus:ring-blue-500 focus:border-blue-500'
    .grid.grid-cols-2
      %div
        %button.btn.text-white.focus:ring-4.focus:outline-none.font-medium.rounded-lg.text-sm.px-5.my-2.text-center.bg-primary-600.hover:bg-primary-700.focus:ring-primary-800
          = f.submit "Post!", class: 'py-2.5', role: "button"
      %div.text-base
        = f.hidden_field :pet_id, value: current_selected.id