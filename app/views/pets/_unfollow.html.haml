
= form_with(model: current_selected.active_relationships.find_by(followed_id: @pet.id), html: { method: :delete }, data: { remote: true } ) do |f|
  %button.text-white.focus:ring-4.focus:outline-none.font-medium.rounded-lg.text-sm.px-3.text-center.inline-flex.items-center.dfocus:ring-blue-800{:class => "py-1.5 !bg-blue-800 hover:!bg-blue-900", :type => "button"}
    = f.submit "Unfollow", class: "px-4 pr-10", role: "button"
